'use strict';

const expect = require('chai').expect;

describe('# 42 is 42', () => {
  it('should equal 42', () => {
    expect(42).to.equal(42);
  });
});

describe('# 0 is 0', () => {
  it('should equal 0', () => {
    console.log("peace and prosperity")
    expect(0).to.equal(42);
  });
});
